package saga.eventuate.tram.travelservice.model;

public enum RejectionReason {
    NO_HOTEL_AVAILABLE, NO_FLIGHT_AVAILABLE, HOTEL_CANCELLATION_REJECTED, FLIGHT_CANCELLATION_REJECTED, REASON_UNKNOWN
}
